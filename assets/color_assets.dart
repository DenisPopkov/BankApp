import 'package:flutter/material.dart';

class ColorAssets {
  Color containerColor = const Color.fromARGB(255, 250, 250, 250);
  Color chipContainer = const Color.fromARGB(20, 0, 0, 0);
  Color secondaryColor = const Color.fromARGB(140, 0, 0, 0);
  Color whiteColor = Colors.white;
  Color blackColor = Colors.black;
}
